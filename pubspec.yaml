name: tmdb_test
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Used for to generate a quick splashscreen.
  animated_splash_screen: ^1.1.0
  # Used to manage the state of the application.
  provider: ^5.0.0
  # Used to for accelerate front-end development.
  simple_code: ^6.2.3
  # Used for application integration with parse server.
  parse_server_sdk: ^3.1.0
  # Used to generate internationalized texts.
  intl_utils: ^2.4.0
  # Used to store non-critical data.
  shared_preferences: ^2.0.6
  # Used to make http/https requests.
  http: ^0.13.3
  # Used to display .svg files.
  flutter_svg: ^0.22.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/appLogo/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_intl:
  enabled: true
